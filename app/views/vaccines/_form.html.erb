<%= form_with(model: vaccine) do |form| %>
  <% if vaccine.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(vaccine.errors.count, "error") %> prohibited this vaccine from being saved:</h2>

      <ul>
        <% vaccine.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :frequency_id, style: "display: block" %>
    <%= form.collection_select :frequency_id, Frequency.all, :id, :how_often, { prompt: "Select a frequency" } %>
  </div>

  <div>
    <%= form.label :mandatory, style: "display: block" %>
    <%= form.checkbox :mandatory %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
